/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname: "usable-husky-702.convex.cloud"}
        ],
    },
};

export default nextConfig;
